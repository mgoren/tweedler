<% if user_signed_in? %>
  <%= link_to "Tweedify", new_tweed_path, class: "btn btn-info", id: "tweedify-btn", remote: true %>
<% end %>

<ul class="list-unstyled" id="tweeds">
  <%= render @tweeds.reverse %>
</ul>
